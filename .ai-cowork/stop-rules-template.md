# Stop rules — {{PROJECT_OR_SPRINT}}

Active from {{DATE}} until revoked.

- {{e.g. No threshold edits until contract is explicit}}
- {{e.g. No canonical output writes from agents}}
- {{e.g. No full parity suite until local invariant passes}}

## Resume conditions

{{When each stop rule lifts}}

## Violation protocol

If a stop rule is violated: halt, document in Decision Board, restore from VCS if needed.
