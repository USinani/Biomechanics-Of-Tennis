# Tennis_Biomechanics_Model_FIXED (deprecated)

MATLAB sources for this project now live under the **Matlab_v2** package root:

- `dynamics/` — two-link matrices, forward dynamics, FK / EE velocity  
- `legacy/` — three-link model, ball tools, inverse-dynamics tennis demo  
- `controls/`, `analysis/`, `export/`, `experiments/`, `utils/`

Add the package to your path from the **Matlab_v2** folder (`main.m` or `addpath(genpath(pwd))`). Do not rely on this folder for code.
